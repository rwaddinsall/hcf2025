---
import "../styles/tailwind.css";
import "../assets/scss/index.scss";
import SiteMeta from "../components/SiteMeta.astro";
import Footer from "../components/Footer.astro";
import Navigation from "@components/Navigation.astro";


const {
  title = "Hopkins Creek Festival",
  description = "Hopkins Creek Music Festival - A celebration of music, art, and community in the heart of nature. Join us for an unforgettable weekend of incredible artists and good vibes.",
  url = Astro.site,
  image = "social-preview-image.png",
  author = "Hopkins Creek Festival",
} = Astro.props;
---

<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />

    <!-- favicon -->
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />

    <SiteMeta
      title={title}
      description={description.substring(0, 100)}
      url={url}
      image={image}
      author={author}
    />
  </head>
  <body>
    <Navigation />
    <main id="main-content">
      <div class="container mx-auto px-6 md:px-8 py-12 md:py-20 flex flex-col items-center">
        <slot />
      </div>
    </main>
    <Footer />
    <style lang="scss" is:global>
      // sticky footer on low content pages
      html,
      body {
        height: 100%;
      }

      body {
        display: flex;
        flex-direction: column;
        overflow-x: hidden; // Prevent horizontal scroll
        max-width: 100vw; // Ensure body doesn't exceed viewport width
        // Removed conflicting padding - let components handle their own spacing

        main {
          flex: 1 0 auto;
          max-width: var(--grid-max-width);
          margin: var(--space-s) auto 0; // Reduced top margin for tighter spacing from nav
          // Removed conflicting padding-inline - components handle their own horizontal spacing
          // Removed vertical padding - components handle their own vertical spacing
        }

        footer {
          flex-shrink: 0;
          margin-top: var(
            --space-xl
          ); // Add top margin for separation from content
        }
      }

      pre {
        border: 2px solid var(--link-color);
        border-radius: 0.35rem;
        padding: 1rem;
      }
    </style>
    <script is:inline src="https://cdn.jsdelivr.net/npm/flowbite@3.1.2/dist/flowbite.min.js"></script>
  </body>
</html>
